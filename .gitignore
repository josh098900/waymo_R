# Data — too large for git, regenerable from source
data/raw/
data/processed/
*.tfrecord
*.parquet

# R session
.Rhistory
.Rapp.history
.RData
.RDataTmp
.Ruserdata

# RStudio
.Rproj.user/

# renv (keeps lockfile, ignores the actual library)
renv/library/
renv/local/
renv/cellar/
renv/staging/
renv/python/

# Python (for the conversion script later)
__pycache__/
*.pyc
.venv/
venv/

# OS junk
.DS_Store
Thumbs.db

# Quarto / Rmd render artifacts
*_files/
*_cache/
